import React from "react";
import {SearchButton, SearchIcon} from "../Server/ServerList/styles";

export const DiscoverView: React.FC = () => {

    return (
        <SearchButton>
            <SearchIcon/>
        </SearchButton>
    )
};